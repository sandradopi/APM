<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.findmyrhythm">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->

    <uses-permission android:name="android.providers.gsf.permission.READ_GSERVICES"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.WAKE_LOCK"/>


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <service android:name=".Model.EndlessService"
            android:enabled="true"
            android:exported="false">

        </service>
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".View.SearchEventsActivity"
            android:label="@string/title_activity_search_events"></activity>
        <activity
            android:name=".View.SelectAddressOnMapActivity"
            android:label="@string/title_activity_search_events"></activity>
        <activity android:name=".View.LocationsSettingsActivity" />
        <activity android:name=".View.GenresSettingsActivity" />
        <activity
            android:name=".View.SplashActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".View.OrganizerLogActivity" />
        <activity android:name=".View.UserLogActivity" />
        <activity android:name=".View.SetGenresActivity" />
        <activity
            android:name=".View.SetLocationActivity">
        </activity>
        <activity
            android:name=".View.GreetingsActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".View.OrganizerInfoDialog"
            android:theme="@android:style/Theme.Dialog" />
        <activity
            android:name=".View.OrganizerProfileActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".View.LoginActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
        </activity>

        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/facebook_app_id" />

        <activity
            android:name="com.facebook.FacebookActivity"
            android:configChanges="keyboard|keyboardHidden|screenLayout|screenSize|orientation"
            android:label="@string/app_name" />
        <activity
            android:name="com.facebook.CustomTabActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data android:scheme="@string/fb_login_protocol_scheme" />
            </intent-filter>
        </activity>
        <activity
            android:name=".View.UserProfileActivity"
            android:label="@string/title_activity_perfil_usuario_tabs"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".View.SearchActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".View.RecommendedEventsActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".View.CreateEventActivity" />
        <activity
            android:name=".View.OrganizerSettingsActivity"
            android:label="@string/title_activity_ajustes_organizador"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".View.UserSettingsActivity"
            android:label="@string/title_activity_ajustes_usuario"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".View.EventInfoActivity" />
        <activity android:name=".View.OrganizerEventInfoActivity" />
        <activity android:name=".View.FinishedEventInfoActivity" />
    </application>

</manifest>